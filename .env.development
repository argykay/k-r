# Local dev: serve at http://localhost:3000/
PUBLIC_URL=

# Dev guest password (override or clear in .env.development.local).
# Restart `npm start` after changing — CRA does not hot-reload env files.
REACT_APP_SITE_PASSWORD=test
